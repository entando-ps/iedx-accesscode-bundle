code: accesscode-bundle
description: This is the accesscode bundle

components:
  plugins:
    - plugins/accesscode-plugin.yaml

  widgets:
    - ui/widgets/widget-dir/fe-react-app/iedx-widget-descriptor.yaml
# entando-needle-descriptor-add-widgets - Entando blueprint will add widget bundle files here

